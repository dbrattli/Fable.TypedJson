.DS_Store
.claude/
*.ez
erl_crash.dump
**/bin/
**/obj/
**/build/
apps/
_build/

# Ignore compiled BEAM files
*.beam

# Paket
paket-files/
.paket/

# NuGet Packages
*.nupkg
*.snupkg
nupkgs/

.fake

# Python
.venv
**/__pycache__/
.pytest_cache

# Benchmark run output — regenerated by every `dotnet run` of the benchmark
# project. Kept out of the repo; a baseline worth keeping should be recorded
# deliberately, not carried in as run logs.
BenchmarkDotNet.Artifacts/
*.binlog
