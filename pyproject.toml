[project]
name = "fable-typedjson"
version = "0.1.0"
description = "Fable.TypedJson — Python runtime support and tests"
requires-python = ">= 3.12, < 4"
license = "MIT"
dependencies = [
    "fable-library==5.16.0",
]
