{erl_opts, [debug_info]}.

%% The BEAM test project references only the Beam shim and its bindings, so
%% everything Fable emits into fable_modules/ belongs in the Erlang build.
{project_app_dirs, [".", "fable_modules/*"]}.

{deps, [
    {jsx, "3.1.0"}
]}.
